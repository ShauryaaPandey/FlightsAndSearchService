/ rolebased
  - this src/app same 
  - src/
     index.js //server
     models
     controllers
     middlewares
     services
     utils
     config
     repository
  - tests/[later]
  - static
  - temp

anything that we dont want to expose is put inside .gitignore(which is put outside the src folder) 




/ feature based
   -flights
      /models
      /controller
   -search
       /models
       /controllers    